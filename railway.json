{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd website.client && npm install && npm run build && cd ../website.server && dotnet restore && dotnet publish -c Release -o out"
  },
  "deploy": {
    "startCommand": "cd website.server && dotnet out/website.Server.dll",
    "healthcheckPath": "/health",
    "ports": [
      {
        "port": 10000,
        "protocol": "http"
      }
    ]
  }
}